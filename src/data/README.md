`world-map.json` is a geographic asset: an SVG viewBox and one path per country,
with ISO 3166-1 alpha-2 codes and English names. It is map geometry, not product
code — there is nothing in it to translate, and redrawing the world by hand to
avoid copying a public-domain outline would be theatre rather than diligence.

Everything else in this application is written here.
